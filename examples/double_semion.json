{
  "schema_version": 1,
  "name": "Double semion",
  "symbol_format": "functions",
  "anyons": {
    "type": "tuples",
    "moduli": [
      2,
      2
    ]
  },
  "vacuum": [
    0,
    0
  ],
  "fusion_rules": {
    "expression": "1 if c == ((a[0] + b[0]) % 2, (a[1] + b[1]) % 2) else 0"
  },
  "quantum_dimensions": {
    "expression": "1"
  },
  "topological_spins": {
    "expression": "exp(pi * 1j * (a[0] * a[0] - a[1] * a[1]) / 2)"
  },
  "F_symbols": {
    "expression": "(-1) ** (a[0] * ((b[0] + c[0]) // 2) - a[1] * ((b[1] + c[1]) // 2))"
  },
  "R_symbols": {
    "expression": "exp(pi * 1j * (a[0] * b[0] - a[1] * b[1]) / 2)"
  },
  "symmetry": {
    "group": {
      "gap": "CyclicGroup(2)",
      "generator_names": [
        "S"
      ]
    },
    "rho": {
      "generator_images": [
        1
      ]
    },
    "action": {
      "expression": "(a[1], a[0]) if g == 'S' else a"
    },
    "U_symbols": {
      "expression": "1"
    },
    "eta_symbols": {
      "expression": "1"
    }
  }
}
